import React from "react";
import ReactDOM from "react-dom/client";
import NashestvieApp from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <NashestvieApp />
  </React.StrictMode>
);
